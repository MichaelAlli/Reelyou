export interface MySkyViewportSnapshot {
  offsetX: number;
  offsetY: number;
  scale: number;
}

export const DEFAULT_MY_SKY_VIEWPORT: MySkyViewportSnapshot = {
  offsetX: 0,
  offsetY: 0,
  scale: 1,
};
