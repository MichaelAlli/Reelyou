/**
 * Canonical bottom navigation policy for Skywrite composer (/skywrite/compose).
 * Implementation: shared `BottomNav` in `SkywriteScreen` — do not remove or duplicate.
 */
export const SKYWRITE_COMPOSE_REQUIRES_CANONICAL_BOTTOM_NAV = true as const;
