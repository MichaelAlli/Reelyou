import { memo } from 'react';

/** Main golden path is rendered in the master background artwork — no overlay path. */
function JourneyPathComponent() {
  return null;
}

export const JourneyPath = memo(JourneyPathComponent);
export const StarPathCentralTrail = JourneyPath;
