/**
 * TEMPORARY auth assets — pending approval before moving into permanent asset structure.
 * signup-day-landscape-TEMP.png: attached clean daytime mountain/lake background from Cursor chat.
 */
export const AuthTempAssets = {
  signupDayLandscape: require('../assets/temp/signup-day-landscape-TEMP.png'),
} as const;
