/**
 * StarPath master cinematic background — original clean asset (locked).
 */
export const StarPathAssets = {
  cinematicMaster: require('../assets/backgrounds/starpath/starpath-cinematic-master.png'),
} as const;
