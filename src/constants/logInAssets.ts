/**
 * Sign In background assets — separate from locked Sign Up background assets.
 * loginDayBackground: attached clean daytime landscape (JPEG, 576×1024).
 * loginNightBackground: attached clean cinematic nighttime landscape (PNG).
 */
export const LogInAssets = {
  loginDayBackground: require('../assets/backgrounds/reelyou-login-day-background.jpg'),
  loginNightBackground: require('../assets/backgrounds/reelyou-login-night-background.png'),
} as const;
